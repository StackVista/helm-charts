apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-management.fullname" . }}
  labels:
    {{- include "tenant-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tenant-management.selectorLabels" . | nindent 4 }}
