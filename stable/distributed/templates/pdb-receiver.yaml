{{- define "distributed.receiver.poddisruptionbudget" -}}
metadata:
  name: {{ template "common.fullname" . }}-receiver
  labels:
    app.kubernetes.io/component: receiver
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: receiver
{{- with .Values.stackstate.components.receiver.poddisruptionbudget }}
  {{ toYaml . | nindent 2 }}
{{- end }}
{{- end -}}

{{- $commonPdb := fromYaml (include "common.poddisruptionbudget" .) -}}
{{- $receiverPdb := fromYaml (include "distributed.receiver.poddisruptionbudget" .) -}}
{{- toYaml (merge $receiverPdb $commonPdb) -}}
