{{- define "stackstate.kafka2prom.secret" -}}
metadata:
  name: {{ template "common.fullname.short" . }}-kafka2prom
{{- include "stackstate.service.secret.data" (dict "service" .Values.stackstate.components.kafka2prom "context" $) | nindent 0 -}}
{{- end -}}

{{- $commonSecret := fromYaml (include "common.secret" .) -}}
{{- $stackstateKafka2PromSecret := fromYaml (include "stackstate.kafka2prom.secret" .) -}}
{{- toYaml (merge $stackstateKafka2PromSecret $commonSecret) -}}
