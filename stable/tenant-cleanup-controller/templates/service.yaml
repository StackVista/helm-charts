apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-cleanup-controller.fullname" . }}
  labels:
    {{- include "tenant-cleanup-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tenant-cleanup-controller.selectorLabels" . | nindent 4 }}
