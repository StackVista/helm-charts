{{- define "stackstate.sync.txlog.persistentvolumeclaim" -}}
metadata:
  name: {{ template "common.fullname.short" . }}-sync-txlog
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ include "suse-observability.features.get" (dict "key" "storeTransactionLogsToPVC.volumeSize" "context" .) | trim }}
{{- $storageClass := include "suse-observability.features.get" (dict "key" "storeTransactionLogsToPVC.storageClass" "context" .) | trim }}
{{- if and $storageClass (ne $storageClass "<no value>") }}
  storageClassName: {{ $storageClass }}
{{- end }}
{{- end -}}

{{- define "stackstate.sync.tmp.persistentvolumeclaim" -}}
metadata:
  name: {{ template "common.fullname.short" . }}-sync-tmp
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ include "suse-observability.global.syncTmpToPVCVolumeSize" . | default .Values.stackstate.components.sync.tmpToPVC.volumeSize }}
  {{ include "common.storage.class" ( dict "persistence" .Values.stackstate.components.sync.tmpToPVC "global" .Values.global) | nindent 2 }}
{{- end -}}

{{- if and (include "suse-observability.features.enabled" (dict "key" "storeTransactionLogsToPVC.enabled" "context" .)) (include "suse-observability.features.enabled" (dict "key" "server.split" "context" .)) }}
{{- $commonPVC := fromYaml (include "common.persistentvolumeclaim" .) -}}
{{- $txlogPVC := fromYaml (include "stackstate.sync.txlog.persistentvolumeclaim" .) -}}
{{- toYaml (merge $txlogPVC $commonPVC) -}}
{{- end -}}

{{- if and .Values.stackstate.components.sync.tmpToPVC (include "suse-observability.features.enabled" (dict "key" "server.split" "context" .)) }}
---
{{ $commonPVC := fromYaml (include "common.persistentvolumeclaim" .) -}}
{{- $tmpPVC := fromYaml (include "stackstate.sync.tmp.persistentvolumeclaim" .) -}}
{{- toYaml (merge $tmpPVC $commonPVC) }}
{{- end -}}
