{{- if and (include "suse-observability.features.enabled" (dict "key" "server.split" "context" .)) .Values.stackstate.k8sAuthorization.enabled }}
{{- $valuesPdb := dict "PdbName" "authorization-sync" "PdbBudget" .Values.stackstate.components.authorizationSync.poddisruptionbudget -}}
{{- (include "stackstate.service.poddisruptionbudget" (merge $valuesPdb .)) -}}
{{- end }}
