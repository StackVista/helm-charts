{{- define "stackstate.health-sync.persistentvolumeclaim.txLog" -}}
{{- $storageClass := include "suse-observability.features.get" (dict "key" "storeTransactionLogsToPVC.storageClass" "context" . ) }}
metadata:
  name: {{ template "common.fullname.short" . }}-health-sync-txlog
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ include "suse-observability.features.get" (dict "key" "storeTransactionLogsToPVC.volumeSize" "context" . )}}
{{- if ne $storageClass "<no value>" }}
  storageClassName: {{ $storageClass }}
{{- end }}
{{- end -}}

{{- if and (include "suse-observability.features.enabled" (dict "key" "storeTransactionLogsToPVC.enabled" "context" .)) (include "suse-observability.features.enabled" (dict "key" "server.split" "context" .)) }}
{{- $commonPVC := fromYaml (include "common.persistentvolumeclaim" .) -}}
{{- $stackstateHealthSyncPVCTxLog := fromYaml (include "stackstate.health-sync.persistentvolumeclaim.txLog" .) -}}
{{- toYaml (merge $stackstateHealthSyncPVCTxLog $commonPVC) -}}
{{- end -}}

{{- if and .Values.stackstate.components.healthSync.tmpToPVC (include "suse-observability.features.get" (dict "key" "server.split" "context" .)) }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "common.fullname.short" . }}-health-sync-tmp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.stackstate.components.healthSync.tmpToPVC.volumeSize }}
  {{ include "common.storage.class" ( dict "persistence" .Values.stackstate.components.healthSync.tmpToPVC "global" .Values.global) | nindent 2 }}
{{- end}}
