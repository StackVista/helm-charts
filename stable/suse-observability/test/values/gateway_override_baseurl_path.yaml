# Gateway enabled with path override
global:
  suseObservability:
    license: "xxxx-xxxx-xxxx"
    baseUrl: https://test.example.com/observability
    adminPassword: "testpassword"
    sizing:
      profile: "10-nonha"
gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
  path: /api
