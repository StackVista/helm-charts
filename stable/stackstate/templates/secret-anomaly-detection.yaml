{{ if (get .Values "anomaly-detection").enabled }}
{{ $params := dict }}
{{ $instance_name := (include "common.fullname.short" .) -}}
{{$_ := set $params "instance" (list $instance_name "-server:7070" | join "") -}}
{{$_ := set $params "receiverApiKey" (include "receiver.apiKey" .) -}}
{{$_ := set $params "elasticSearchUri" (include "stackstate.es.endpoint" .) -}}
{{ include "anomaly-detection.configuration.renderer" (list . $params (get .Values "anomaly-detection").stackstate) }}
{{- end -}}
