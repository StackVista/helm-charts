{{- define "stackstate.k2es.secret" -}}
metadata:
  name: {{ template "common.fullname.short" . }}-k2es
data:
{{- range $key, $value := .Values.stackstate.components.k2es.extraEnv.secret }}
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}
{{- end -}}

{{- if .Values.stackstate.components.k2es.extraEnv.secret -}}
{{- $commonSecret := fromYaml (include "k2es.secret" .) -}}
{{- $stackstateK2esSecret := fromYaml (include "stackstate.k2es.secret" .) -}}
{{- toYaml (merge $stackstateK2esSecret $commonSecret) -}}
{{- end -}}
