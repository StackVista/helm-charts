{{- if .Values.stackstate.components.all.image.pullSecretDockerConfigJson }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "common.fullname.short" . }}-pull-secret
data:
  .dockerconfigjson: {{ .Values.stackstate.components.all.image.pullSecretDockerConfigJson }}
type: kubernetes.io/dockerconfigjson
{{- end }}
