{{- define "stackstate.processor.servicemonitor" -}}
metadata:
  labels:
    app.kubernetes.io/component: processor
  {{- with .Values.stackstate.components.all.metrics.servicemonitor.additionalLabels }}
    {{ toYaml . | nindent 4 }}
  {{- end }}
  name: {{ template "common.fullname.short" . }}-processor
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: processor
  {{ include "stackstate.servicemonitor.extraLabels" . | nindent 2 }}
{{- end -}}

{{- if .Values.stackstate.server.split }}
{{- if and ( .Values.stackstate.components.all.metrics.enabled ) ( .Values.stackstate.components.all.metrics.servicemonitor.enabled ) }}
{{- $commonServiceMonitor := fromYaml (include "common.servicemonitor" .) -}}
{{- $processorServiceMonitor := fromYaml (include "stackstate.processor.servicemonitor" .) -}}
{{- toYaml (merge $processorServiceMonitor $commonServiceMonitor) -}}
{{- end -}}
{{- end }}
