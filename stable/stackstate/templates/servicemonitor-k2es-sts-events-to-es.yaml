{{- define "stackstate.k2es.stsEventsToEs.servicemonitor" -}}
metadata:
  labels:
    app.kubernetes.io/component: sts2es
  {{- with .Values.stackstate.components.all.metrics.servicemonitor.additionalLabels }}
    {{ toYaml . | nindent 4 }}
  {{- end }}
  name: {{ template "common.fullname.short" . }}-sts2es
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: sts2es
  {{ include "stackstate.servicemonitor.extraLabels" . | nindent 2 }}
{{- end -}}

{{- if and ( .Values.stackstate.components.all.metrics.enabled ) ( .Values.stackstate.components.all.metrics.servicemonitor.enabled ) }}
{{- $commonServiceMonitor := fromYaml (include "common.servicemonitor" .) -}}
{{- $k2esStsEventsToEsServiceMonitor := fromYaml (include "stackstate.k2es.stsEventsToEs.servicemonitor" .) -}}
{{- toYaml (merge $k2esStsEventsToEsServiceMonitor $commonServiceMonitor) -}}
{{- end -}}
