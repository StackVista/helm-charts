{{- define "hbase.hdfs.namenode.service" -}}
metadata:
  name: {{ template "common.fullname" . }}-hdfs-nn
  labels:
    app.kubernetes.io/component: hdfs-nn
spec:
  ports:
  - port: 9000
    name: nn
  - port: 50070
    name: nninfo
  selector:
    app.kubernetes.io/component: hdfs-nn
{{- end -}}

{{- $commonService := fromYaml (include "common.service" .) -}}
{{- $stackstateHbaseHdfsDatanodeService := fromYaml (include "hbase.hdfs.namenode.service" .) -}}
{{- toYaml (merge $stackstateHbaseHdfsDatanodeService $commonService) -}}
