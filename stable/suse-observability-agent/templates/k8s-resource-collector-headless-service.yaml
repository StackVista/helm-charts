{{- if and (include "stackstate-k8s-agent.k8sResourceCollector.enabled" .) .Values.k8sResourceCollector.leaderElection.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-k8s-resource-collector-headless
  labels:
{{ include "stackstate-k8s-agent.labels" . | indent 4 }}
{{ include "stackstate-k8s-agent.global.extraLabels" . | indent 4 }}
    app.kubernetes.io/component: k8s-resource-collector
  annotations:
{{ include "stackstate-k8s-agent.global.extraAnnotations" . | indent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.k8sResourceCollector.peerSync.port }}
      targetPort: {{ .Values.k8sResourceCollector.peerSync.port }}
      protocol: TCP
      name: peer-sync
  selector:
    app.kubernetes.io/component: k8s-resource-collector
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ include "stackstate-k8s-agent.name" . }}
{{- end }}
