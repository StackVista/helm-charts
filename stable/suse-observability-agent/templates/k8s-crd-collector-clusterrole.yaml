{{- if include "stackstate-k8s-agent.k8sCrdCollector.enabled" . }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Release.Name }}-k8s-crd-collector
  labels:
{{ include "stackstate-k8s-agent.labels" . | indent 4 }}
{{ include "stackstate-k8s-agent.global.extraLabels" . | indent 4 }}
    app.kubernetes.io/component: k8s-crd-collector
  annotations:
{{ include "stackstate-k8s-agent.global.extraAnnotations" . | indent 4 }}
rules:
  # Permission to watch CustomResourceDefinitions
  - apiGroups: ["apiextensions.k8s.io"]
    resources: ["customresourcedefinitions"]
    verbs: ["get", "list", "watch"]

{{- if .Values.k8sCrdCollector.rbac.useWildcard }}
  # Wildcard permissions for watching all custom resources
  # Use this for dynamic CRD discovery in environments with many CRDs
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["get", "list", "watch"]
{{- else }}
  # Restricted permissions for specific API groups
  # Configure .Values.k8sCrdCollector.rbac.apiGroups to specify which groups to watch
{{- range .Values.k8sCrdCollector.rbac.apiGroups }}
  - apiGroups: ["{{ . }}"]
    resources: ["*"]
    verbs: ["get", "list", "watch"]
{{- end }}
{{- end }}
{{- end }}
