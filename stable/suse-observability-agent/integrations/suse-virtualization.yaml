# Integration overlay for the SUSE Virtualization (KubeVirt) stackpack.
#
# Pre-configures the otel.k8sResourceCollector with the API groups consumed by the
# SUSE Virtualization stackpack's component- and relation-mappings.
#
# Usage — enable the flag in your values:
#   otel:
#     k8sResourceCollector:
#       integrations:
#         suseVirtualization: true
otel:
  k8sResourceCollector:
    crdDiscovery:
      apiGroupFilters:
        include:
          "kubevirt.io": true
    rbac:
      # Only consulted when otel.k8sResourceCollector.rbac.useWildcard is false.
      crdApiGroups:
        "kubevirt.io": true
