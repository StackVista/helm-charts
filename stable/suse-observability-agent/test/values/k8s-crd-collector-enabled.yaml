stackstate:
  apiKey: test-api-key
  cluster:
    name: test-cluster
  # Canonical URL shape per the kubernetes-v2 stackpack instructions.
  url: https://my-suse-observability-instance.com/receiver/stsAgent

# Enable cluster collector with default settings (watch all CRDs)
k8sCrdCollector:
  enabled: true
  crdDiscovery:
    snapshotInterval: 5m
    discoveryMode: api_groups
    apiGroupFilters:
      include:
        - "*"
      exclude: []
  rbac:
    useWildcard: true
