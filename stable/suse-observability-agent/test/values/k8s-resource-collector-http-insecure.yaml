stackstate:
  apiKey: test-api-key
  cluster:
    name: test-cluster
  url: https://my-suse-observability-instance.com

# Enable cluster collector with a plain http:// OTLP endpoint (no TLS).
otel:
  enabled: true
  k8sResourceCollector:
    enabled: true
    crDiscovery:
      interval: 15m
      discoveryMode: all
    rbac:
      useWildcard: true

  platformHttpOtlpEndpoint: "http://otlp-my-instance.example.com:4318"
