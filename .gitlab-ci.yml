################################################################
# THIS FILE WAS GENERATED AUTOMATICALLY! DO NOT EDIT MANUALLY! #
################################################################
image: quay.io/helmpack/chart-testing:v3.10.1
integration_test_stackstate:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate/**/*
      exists:
        - stable/stackstate/itest/*.go
      if: $CI_COMMIT_TAG
  script:
    - go test ./stable/stackstate/itest/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate
integration_test_stackstate_k8s:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate-k8s/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate-k8s/%
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-k8s/**/*
      exists:
        - stable/stackstate-k8s/itest/*.go
      if: $CI_COMMIT_TAG
  script:
    - go test ./stable/stackstate-k8s/itest/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-k8s
push_artifactory-cleaner_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/artifactory-cleaner/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-artifactory-cleaner]/
      when: on_success
    - changes:
        - stable/artifactory-cleaner/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^artifactory-cleaner\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/artifactory-cleaner
push_artifactory-cleaner_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_artifactory-cleaner_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/artifactory-cleaner/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-artifactory-cleaner]/
      when: on_success
    - changes:
        - stable/artifactory-cleaner/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^artifactory-cleaner\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/artifactory-cleaner
push_aws-amicleaner_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-amicleaner/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-aws-amicleaner]/
      when: on_success
    - changes:
        - stable/aws-amicleaner/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^aws-amicleaner\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/aws-amicleaner
push_aws-nuke_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-nuke/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-aws-nuke]/
      when: on_success
    - changes:
        - stable/aws-nuke/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^aws-nuke\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/aws-nuke
push_aws-pod-identity-webhook_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-pod-identity-webhook/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-aws-pod-identity-webhook]/
      when: on_success
    - changes:
        - stable/aws-pod-identity-webhook/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^aws-pod-identity-webhook\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/aws-pod-identity-webhook
push_beacher-job_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/beacher-job/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-beacher-job]/
      when: on_success
    - changes:
        - stable/beacher-job/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^beacher-job\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/beacher-job
push_chartmuseum_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/chartmuseum/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-chartmuseum]/
      when: on_success
    - changes:
        - stable/chartmuseum/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^chartmuseum\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/chartmuseum
push_common_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/common/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-common]/
      when: on_success
    - changes:
        - stable/common/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^common\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/common
push_common_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_common_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/common/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-common]/
      when: on_success
    - changes:
        - stable/common/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^common\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/common
push_elasticsearch_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/elasticsearch/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-elasticsearch]/
      when: on_success
    - changes:
        - stable/elasticsearch/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^elasticsearch\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/elasticsearch
push_elasticsearch_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_elasticsearch_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/elasticsearch/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-elasticsearch]/
      when: on_success
    - changes:
        - stable/elasticsearch/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^elasticsearch\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/elasticsearch
push_falco-reporter_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/falco-reporter/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-falco-reporter]/
      when: on_success
    - changes:
        - stable/falco-reporter/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^falco-reporter\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/falco-reporter
push_gitlab-steward_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/gitlab-steward/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-gitlab-steward]/
      when: on_success
    - changes:
        - stable/gitlab-steward/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^gitlab-steward\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/gitlab-steward
push_hbase_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/hbase/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-hbase]/
      when: on_success
    - changes:
        - stable/hbase/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^hbase\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/hbase
push_hbase_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_hbase_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/hbase/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-hbase]/
      when: on_success
    - changes:
        - stable/hbase/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^hbase\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/hbase
push_helm-reaper_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/helm-reaper/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-helm-reaper]/
      when: on_success
    - changes:
        - stable/helm-reaper/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^helm-reaper\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/helm-reaper
push_iceman_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/iceman/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-iceman]/
      when: on_success
    - changes:
        - stable/iceman/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^iceman\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/iceman
push_k8s-image-list-to-s3_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/k8s-image-list-to-s3/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-k8s-image-list-to-s3]/
      when: on_success
    - changes:
        - stable/k8s-image-list-to-s3/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^k8s-image-list-to-s3\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/k8s-image-list-to-s3
push_k8s-spot-termination-handler_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/k8s-spot-termination-handler/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-k8s-spot-termination-handler]/
      when: on_success
    - changes:
        - stable/k8s-spot-termination-handler/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^k8s-spot-termination-handler\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/k8s-spot-termination-handler
push_kafkaup-operator_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/kafkaup-operator/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-kafkaup-operator]/
      when: on_success
    - changes:
        - stable/kafkaup-operator/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^kafkaup-operator\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/kafkaup-operator
push_kafkaup-operator_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_kafkaup-operator_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/kafkaup-operator/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-kafkaup-operator]/
      when: on_success
    - changes:
        - stable/kafkaup-operator/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^kafkaup-operator\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/kafkaup-operator
push_kommoner-operator_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/kommoner-operator/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-kommoner-operator]/
      when: on_success
    - changes:
        - stable/kommoner-operator/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^kommoner-operator\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/kommoner-operator
push_minio_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/minio/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-minio]/
      when: on_success
    - changes:
        - stable/minio/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^minio\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/minio
push_minio_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_minio_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/minio/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-minio]/
      when: on_success
    - changes:
        - stable/minio/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^minio\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/minio
push_otel-demo_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/otel-demo/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-otel-demo]/
      when: on_success
    - changes:
        - stable/otel-demo/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^otel-demo\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/otel-demo
push_petros-d-kubelet-stats-exporter_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/petros-d-kubelet-stats-exporter/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-petros-d-kubelet-stats-exporter]/
      when: on_success
    - changes:
        - stable/petros-d-kubelet-stats-exporter/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^petros-d-kubelet-stats-exporter\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/petros-d-kubelet-stats-exporter
push_pull-secret_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/pull-secret/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-pull-secret]/
      when: on_success
    - changes:
        - stable/pull-secret/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^pull-secret\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/pull-secret
push_pull-secret_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_pull-secret_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/pull-secret/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-pull-secret]/
      when: on_success
    - changes:
        - stable/pull-secret/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^pull-secret\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/pull-secret
push_receiveramplifier_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/receiveramplifier/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-receiveramplifier]/
      when: on_success
    - changes:
        - stable/receiveramplifier/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^receiveramplifier\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/receiveramplifier
push_redirector_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/redirector/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-redirector]/
      when: on_success
    - changes:
        - stable/redirector/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^redirector\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/redirector
push_sandbox-operator_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/sandbox-operator/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-sandbox-operator]/
      when: on_success
    - changes:
        - stable/sandbox-operator/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^sandbox-operator\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/sandbox-operator
push_sock-shop_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/sock-shop/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-sock-shop]/
      when: on_success
    - changes:
        - stable/sock-shop/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^sock-shop\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/sock-shop
push_stackstate-agent_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-agent]/
      when: on_success
    - changes:
        - stable/stackstate-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-agent
push_stackstate-agent_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_stackstate-agent_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-agent]/
      when: on_success
    - changes:
        - stable/stackstate-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^stackstate-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate-agent
push_stackstate-k8s-agent_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-k8s-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-k8s-agent]/
      when: on_success
    - changes:
        - stable/stackstate-k8s-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-k8s-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-k8s-agent
push_stackstate-k8s-agent_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_stackstate-k8s-agent_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-k8s-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-k8s-agent]/
      when: on_success
    - changes:
        - stable/stackstate-k8s-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^stackstate-k8s-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate-k8s-agent
push_stackstate-k8s_to_internal:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - .gitlab/configure_git.sh
    - export BRANCHES=${CI_COMMIT_BRANCH:-$(git for-each-ref --format="%(objectname) %(refname:short)" refs/remotes/origin | awk -v branch="$(git rev-parse HEAD)" '$1==branch && $2!="origin" {print $2}' | sed -E "s/^origin\/(.*)$/\1/")}
    - .gitlab/set_sts_chart_master_version.sh stable/stackstate-k8s $(echo $CI_COMMIT_TAG | sed -E 's/^stackstate-k8s\/(.*)$/\1/')
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-k8s/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-k8s]/
      when: on_success
    - changes:
        - stable/stackstate-k8s/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-k8s\/.*/
      when: on_success
  script:
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate-k8s/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate-k8s/%
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
    - .gitlab/bump_sts_chart_master_version_v2.sh stable/stackstate-k8s
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-k8s
push_stackstate-monitoring_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-monitoring/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-monitoring]/
      when: on_success
    - changes:
        - stable/stackstate-monitoring/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-monitoring\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-monitoring
push_stackstate-monitoring_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_stackstate-monitoring_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-monitoring/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-monitoring]/
      when: on_success
    - changes:
        - stable/stackstate-monitoring/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^stackstate-monitoring\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate-monitoring
push_stackstate-standalone_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-standalone/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-standalone]/
      when: on_success
    - changes:
        - stable/stackstate-standalone/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-standalone\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-standalone
push_stackstate-standalone_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_stackstate-standalone_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-standalone/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-standalone]/
      when: on_success
    - changes:
        - stable/stackstate-standalone/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^stackstate-standalone\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate-standalone
push_stackstate-values_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-values/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-values]/
      when: on_success
    - changes:
        - stable/stackstate-values/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate-values\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate-values
push_stackstate-values_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_stackstate-values_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-values/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate-values]/
      when: on_success
    - changes:
        - stable/stackstate-values/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^stackstate-values\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate-values
push_stackstate_release_to_internal:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - if: $CI_COMMIT_TAG =~ /^(\d+)\.(\d+)\.(\d+)-pre\.(\d+)\.rc\.(\d+)$/
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^(\d+)\.(\d+)\.(\d+)-rc\.(\d+)$/
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^(\d+)\.(\d+)\.(\d+)$/
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^(\d+)\.(\d+)\.(\d+)-pre\.(\d+)$/
      when: on_success
  script:
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate
push_stackstate_release_to_public:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - if: $CI_COMMIT_TAG =~ /^(\d+)\.(\d+)\.(\d+)$/
      when: on_success
  script:
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/stackstate
push_stackstate_to_internal:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - .gitlab/bump_sts_chart_master_version.sh stackstate-internal stackstate
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-stackstate]/
      when: on_success
    - changes:
        - stable/stackstate/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^stackstate\/.*/
      when: on_success
  script:
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/stackstate
push_suse-observability-agent_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-suse-observability-agent]/
      when: on_success
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^suse-observability-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/suse-observability-agent
push_suse-observability-agent_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_suse-observability-agent_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-suse-observability-agent]/
      when: on_success
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^suse-observability-agent\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/suse-observability-agent
push_suse-observability-agent_to_rancher:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-suse-observability-agent]/
      when: on_success
    - changes:
        - stable/suse-observability-agent/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^suse-observability-agent\/.*/
      when: on_success
  script:
    - .gitlab/publish-suse-agent-to-rancher.sh
  stage: push-charts-to-rancher
  variables:
    CHART: stable/suse-observability-agent
    RANCHER_HELM_REGISTRY_BUCKET: ${RANCHER_HELM_REGISTRY_OPTIMUS_BUCKET}
    RANCHER_HELM_REGISTRY_PASSWORD: ${RANCHER_HELM_REGISTRY_OPTIMUS_PASSWORD}
    RANCHER_HELM_REGISTRY_USERNAME: ${RANCHER_HELM_REGISTRY_OPTIMUS_USERNAME}
push_tenantmanager_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/tenantmanager/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-tenantmanager]/
      when: on_success
    - changes:
        - stable/tenantmanager/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^tenantmanager\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/tenantmanager
push_test_charts:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_COMMIT_TAG
      when: never
    - when: always
  script:
    - source .gitlab/aws_auth_setup.sh
    - sh test/sync-repo.sh
  stage: build
  variables:
    AWS_BUCKET: s3://helm-test.stackstate.io
    REPO_URL: https://helm-test.stackstate.io/
push_trafficmirror_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/trafficmirror/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-trafficmirror]/
      when: on_success
    - changes:
        - stable/trafficmirror/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^trafficmirror\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/trafficmirror
push_trafficmirror_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_trafficmirror_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/trafficmirror/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-trafficmirror]/
      when: on_success
    - changes:
        - stable/trafficmirror/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^trafficmirror\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/trafficmirror
push_victoria-metrics-single_to_internal:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/victoria-metrics-single/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-victoria-metrics-single]/
      when: on_success
    - changes:
        - stable/victoria-metrics-single/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: on_success
    - if: $CI_COMMIT_TAG =~ /^victoria-metrics-single\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_INTERNAL_USERNAME} --password ${CHARTMUSEUM_INTERNAL_PASSWORD} ${CHART} ${CHARTMUSEUM_INTERNAL_URL}
  stage: push-charts-to-internal
  variables:
    CHART: stable/victoria-metrics-single
push_victoria-metrics-single_to_public:
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  needs:
    - push_victoria-metrics-single_to_internal
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/victoria-metrics-single/**/*
      if: $CI_COMMIT_BRANCH == "master" && $CI_COMMIT_AUTHOR == "stackstate-system-user <ops@stackstate.com>"  && $CI_COMMIT_MESSAGE =~ /\[publish-victoria-metrics-single]/
      when: on_success
    - changes:
        - stable/victoria-metrics-single/**/*
      if: $CI_COMMIT_BRANCH == "master"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^victoria-metrics-single\/.*/
      when: on_success
  script:
    - helm dependencies update ${CHART}
    - helm cm-push --username ${CHARTMUSEUM_USERNAME} --password ${CHARTMUSEUM_PASSWORD} ${CHART} ${CHARTMUSEUM_URL}
  stage: push-charts-to-public
  variables:
    CHART: stable/victoria-metrics-single
stages:
  - validate
  - test
  - update
  - build
  - push-charts-to-internal
  - push-charts-to-public
  - push-charts-to-rancher
test_artifactory-cleaner:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/artifactory-cleaner/**/*
      exists:
        - stable/artifactory-cleaner/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/artifactory-cleaner/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/artifactory-cleaner
test_aws-amicleaner:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-amicleaner/**/*
      exists:
        - stable/aws-amicleaner/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/aws-amicleaner/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/aws-amicleaner
test_aws-nuke:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-nuke/**/*
      exists:
        - stable/aws-nuke/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/aws-nuke/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/aws-nuke
test_aws-pod-identity-webhook:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/aws-pod-identity-webhook/**/*
      exists:
        - stable/aws-pod-identity-webhook/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/aws-pod-identity-webhook/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/aws-pod-identity-webhook
test_beacher-job:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/beacher-job/**/*
      exists:
        - stable/beacher-job/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/beacher-job/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/beacher-job
test_chartmuseum:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/chartmuseum/**/*
      exists:
        - stable/chartmuseum/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/chartmuseum/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/chartmuseum
test_common:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/common/**/*
      exists:
        - stable/common/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/common/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/common
test_elasticsearch:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/elasticsearch/**/*
      exists:
        - stable/elasticsearch/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/elasticsearch/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/elasticsearch
test_falco-reporter:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/falco-reporter/**/*
      exists:
        - stable/falco-reporter/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/falco-reporter/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/falco-reporter
test_gitlab-steward:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/gitlab-steward/**/*
      exists:
        - stable/gitlab-steward/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/gitlab-steward/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/gitlab-steward
test_hbase:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/hbase/**/*
      exists:
        - stable/hbase/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/hbase/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/hbase
test_helm-reaper:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/helm-reaper/**/*
      exists:
        - stable/helm-reaper/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/helm-reaper/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/helm-reaper
test_iceman:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/iceman/**/*
      exists:
        - stable/iceman/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/iceman/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/iceman
test_k8s-image-list-to-s3:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/k8s-image-list-to-s3/**/*
      exists:
        - stable/k8s-image-list-to-s3/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/k8s-image-list-to-s3/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/k8s-image-list-to-s3
test_k8s-spot-termination-handler:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/k8s-spot-termination-handler/**/*
      exists:
        - stable/k8s-spot-termination-handler/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/k8s-spot-termination-handler/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/k8s-spot-termination-handler
test_kafkaup-operator:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/kafkaup-operator/**/*
      exists:
        - stable/kafkaup-operator/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/kafkaup-operator/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/kafkaup-operator
test_kommoner-operator:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/kommoner-operator/**/*
      exists:
        - stable/kommoner-operator/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/kommoner-operator/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/kommoner-operator
test_minio:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/minio/**/*
      exists:
        - stable/minio/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/minio/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/minio
test_otel-demo:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/otel-demo/**/*
      exists:
        - stable/otel-demo/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/otel-demo/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/otel-demo
test_petros-d-kubelet-stats-exporter:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/petros-d-kubelet-stats-exporter/**/*
      exists:
        - stable/petros-d-kubelet-stats-exporter/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/petros-d-kubelet-stats-exporter/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/petros-d-kubelet-stats-exporter
test_pull-secret:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/pull-secret/**/*
      exists:
        - stable/pull-secret/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/pull-secret/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/pull-secret
test_receiveramplifier:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/receiveramplifier/**/*
      exists:
        - stable/receiveramplifier/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/receiveramplifier/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/receiveramplifier
test_redirector:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/redirector/**/*
      exists:
        - stable/redirector/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/redirector/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/redirector
test_sandbox-operator:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/sandbox-operator/**/*
      exists:
        - stable/sandbox-operator/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/sandbox-operator/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/sandbox-operator
test_sock-shop:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/sock-shop/**/*
      exists:
        - stable/sock-shop/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/sock-shop/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/sock-shop
test_stackstate:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate/**/*
      exists:
        - stable/stackstate/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate
test_stackstate-agent:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-agent/**/*
      exists:
        - stable/stackstate-agent/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate-agent/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-agent
test_stackstate-k8s-agent:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-k8s-agent/**/*
      exists:
        - stable/stackstate-k8s-agent/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate-k8s-agent/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-k8s-agent
test_stackstate-monitoring:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-monitoring/**/*
      exists:
        - stable/stackstate-monitoring/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate-monitoring/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-monitoring
test_stackstate-standalone:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-standalone/**/*
      exists:
        - stable/stackstate-standalone/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate-standalone/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-standalone
test_stackstate-values:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/stackstate-values/**/*
      exists:
        - stable/stackstate-values/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/stackstate-values/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/stackstate-values
test_suse-observability-agent:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/suse-observability-agent/**/*
      exists:
        - stable/suse-observability-agent/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/suse-observability-agent/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/suse-observability-agent
test_tenantmanager:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/tenantmanager/**/*
      exists:
        - stable/tenantmanager/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/tenantmanager/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/tenantmanager
test_trafficmirror:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/trafficmirror/**/*
      exists:
        - stable/trafficmirror/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/trafficmirror/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/trafficmirror
test_victoria-metrics-single:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
    - helm dependencies update ${CHART}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - changes:
        - stable/victoria-metrics-single/**/*
      exists:
        - stable/victoria-metrics-single/test/*.go
      if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - go test ./stable/victoria-metrics-single/test/...
  stage: test
  variables:
    CGO_ENABLED: 0
    CHART: stable/victoria-metrics-single
update_aad_chart_version:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_AAD_CHART_VERSION
      when: always
  script:
    - .gitlab/update_chart_version.sh stable/stackstate-k8s anomaly-detection $UPDATE_AAD_CHART_VERSION
    - .gitlab/commit_changes_and_push.sh anomaly-detection $UPDATE_AAD_CHART_VERSION
  stage: update
  variables:
    GIT_AUTHOR_EMAIL: sts-admin@stackstate.com
    GIT_AUTHOR_NAME: stackstate-system-user
    GIT_COMMITTER_EMAIL: sts-admin@stackstate.com
    GIT_COMMITTER_NAME: stackstate-system-user
update_stackgraph_version:
  before_script:
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
    - helm repo update
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-helm-test-9f869792
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: always
  script:
    - .gitlab/update_sg_version.sh stable/hbase ""
    - .gitlab/update_sg_version.sh stable/stackstate-k8s "hbase."
    - .gitlab/update_chart_version.sh stable/stackstate-k8s hbase local:stable/hbase
    - .gitlab/commit_changes_and_push.sh StackGraph $UPDATE_STACKGRAPH_VERSION
  stage: update
  variables:
    GIT_AUTHOR_EMAIL: sts-admin@stackstate.com
    GIT_AUTHOR_NAME: stackstate-system-user
    GIT_COMMITTER_EMAIL: sts-admin@stackstate.com
    GIT_COMMITTER_NAME: stackstate-system-user
update_stackpacks_version_to_latest:
  before_script:
    - .gitlab/configure_git.sh
    - export BRANCHES=${CI_COMMIT_BRANCH:-$(git for-each-ref --format="%(objectname) %(refname:short)" refs/remotes/origin | awk -v branch="$(git rev-parse HEAD)" '$1==branch && $2!="origin" {print $2}' | sed -E "s/^origin\/(.*)$/\1/")}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: always
    - when: never
  script:
    - .gitlab/stackstate-k8s/update_stackpacks_version_to_latest.sh
  stage: update
  variables:
    GIT_AUTHOR_EMAIL: sts-admin@stackstate.com
    GIT_AUTHOR_NAME: stackstate-system-user
    GIT_COMMITTER_EMAIL: sts-admin@stackstate.com
    GIT_COMMITTER_NAME: stackstate-system-user
update_stackstate_version_to_latest:
  before_script:
    - .gitlab/configure_git.sh
    - export BRANCHES=${CI_COMMIT_BRANCH:-$(git for-each-ref --format="%(objectname) %(refname:short)" refs/remotes/origin | awk -v branch="$(git rev-parse HEAD)" '$1==branch && $2!="origin" {print $2}' | sed -E "s/^origin\/(.*)$/\1/")}
  image: ${DOCKER_PROXY_URL}/stackstate/sts-ci-images:stackstate-devops-fd4b135a
  rules:
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: always
    - when: never
  script:
    - .gitlab/stackstate-k8s/update_stackstate_version_to_latest.sh
  stage: update
  variables:
    GIT_AUTHOR_EMAIL: sts-admin@stackstate.com
    GIT_AUTHOR_NAME: stackstate-system-user
    GIT_COMMITTER_EMAIL: sts-admin@stackstate.com
    GIT_COMMITTER_NAME: stackstate-system-user
validate_charts:
  before_script:
    - .gitlab/validate_before_script.sh
  environment: stseuw1-sandbox-main-eks-sandbox/${CI_COMMIT_REF_NAME}
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - when: always
  script:
    - ct list-changed --config test/ct.yaml
    - if [[ "${CI_MERGE_REQUEST_TARGET_BRANCH_NAME}" =~ ^releasing*|^developing* ]] || [[ -n "${CI_COMMIT_TAG}" ]] ; then export VERSION_INCREMENT_CHECK="--check-version-increment=false" ; fi
    - ct lint --debug --validate-maintainers=false ${VERSION_INCREMENT_CHECK} --excluded-charts stackstate --excluded-charts stackstate-k8s --excluded-charts gitlab-runner --config test/ct.yaml
    - .gitlab/validate_kubeconform.sh
  stage: validate
validate_stackstate_chart:
  before_script:
    - .gitlab/validate_before_script.sh
    - helm repo add bitnami https://charts.bitnami.com/bitnami
    - helm repo add bitnami-full-archive https://raw.githubusercontent.com/bitnami/charts/archive-full-index/bitnami
    - helm repo add bitnami-pre-2022 https://raw.githubusercontent.com/bitnami/charts/eb5f9a9513d987b519f0ecd732e7031241c50328/bitnami
    - helm repo add elastic https://helm.elastic.co
    - helm repo add opentelemetry https://open-telemetry.github.io/opentelemetry-helm-charts
    - helm repo add prometheus https://prometheus-community.github.io/helm-charts
    - helm repo add stable https://charts.helm.sh/stable
    - helm repo add stackstate https://helm.stackstate.io
    - helm repo add stackstate-internal https://helm-internal.stackstate.io
    - helm repo add stackstate-test https://helm-test.stackstate.io
    - helm repo add victoriametrics https://victoriametrics.github.io/helm-charts
  environment: stseuw1-sandbox-main-eks-sandbox/${CI_COMMIT_REF_NAME}
  rules:
    - if: $UPDATE_STACKGRAPH_VERSION
      when: never
    - if: $UPDATE_AAD_CHART_VERSION
      when: never
    - if: $UPDATE_STACKSTATE_DOCKER_VERSION
      when: never
    - if: $UPDATE_STACKPACKS_DOCKER_VERSION
      when: never
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - when: always
  script:
    - ct list-changed --config test/ct.yaml
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate/%
    - yq e '.dependencies[] | select (.repository == "file*").repository | sub("^file://","")' stable/stackstate-k8s/Chart.yaml  | xargs -I % helm dependencies build stable/stackstate-k8s/%
    - ct lint --debug --validate-maintainers=false --charts stable/stackstate --charts stable/stackstate-k8s --config test/ct.yaml
    - .gitlab/validate_kubeconform.sh
  stage: validate
variables:
  HELM_VERSION: v3.1.2
  PROMOTION_DRY_RUN: "no"
workflow:
  rules:
    - if: $CI_MERGE_REQUEST_IID
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
